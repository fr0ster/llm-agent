import { describe } from 'node:test';

/**
 * @deprecated Legacy pre-streaming suite.
 *
 * This test file targeted the old non-streaming SmartAgent contract and is
 * intentionally skipped. Active coverage is provided by:
 * - regression.test.ts
 * - integration.test.ts
 * - e2e.test.ts
 * - server.test.ts
 */
describe.skip('SmartAgent legacy suite (deprecated)', () => {});
