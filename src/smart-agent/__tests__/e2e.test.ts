import { describe } from 'node:test';

/**
 * @deprecated Legacy E2E scenarios for pre-unification SmartAgent behavior.
 *
 * Assertions in this file depend on old output-shaping and tool-loop semantics.
 * They are intentionally skipped until replaced with E2E scenarios aligned to
 * the current SmartServer + SmartAgent contracts.
 */
describe.skip('SmartAgent legacy e2e suite (deprecated)', () => {});
