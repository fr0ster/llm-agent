import { describe } from 'node:test';

/**
 * @deprecated Legacy integration suite for pre-unification SmartAgent behavior.
 *
 * Current release uses updated orchestration contracts (streaming loop, session
 * diagnostics, and revised policy/injection flow), so these assertions are no
 * longer authoritative and are intentionally skipped.
 */
describe.skip('SmartAgent legacy integration suite (deprecated)', () => {});
