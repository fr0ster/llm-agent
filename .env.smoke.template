# Smoke test — LLM credentials
# Copy this file to .env.smoke and fill in your values

# DeepSeek API
DEEPSEEK_API_KEY=                   # paste your key here
DEEPSEEK_MODEL=deepseek-chat

# Path to ABAP connection env file (passed to mcp-abap-adt --env-path)
ABAP_ENV_PATH=.env.abap
