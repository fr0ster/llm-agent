# Extract OpenAI LLM and Embedder — Design Spec

**Date:** 2026-04-22
**Target release:** v10.1.0 (minor, additive)
**Status:** Draft → In Review
**Builds on:** v10.0.0 (monorepo split)

## Motivation

v10.0.0 split the repository into `@mcp-abap-adt/llm-agent` (core) and `@mcp-abap-adt/llm-agent-server` (runtime), but left LLM providers and embedders inside those two packages. A consumer who only needs one provider (e.g. DeepSeek LLM + Ollama embeddings, a pattern actually used by `examples/docker-deepseek/`) still pulls OpenAI-specific code.

v10.1.0 extracts the first provider family — OpenAI — into dedicated packages, proving the pattern that subsequent minor releases (10.2.0, 10.3.0, …) will repeat for every other provider. The goal is a strictly smaller runtime surface for consumers who don't use OpenAI.

Two separate packages are created (`openai-llm` + `openai-embedder`) rather than one, because the two artifacts are independently useful: a consumer can pair `openai-llm` with any embedder (Ollama, SAP AI Core, local) or pair `openai-embedder` with any LLM. Lump-ing them into one package forces dead weight on mixed-provider deployments.

## Scope

### In scope

- Create `packages/openai-llm/` with `OpenAIProvider` (implements `ILlm`).
- Create `packages/openai-embedder/` with `OpenAiEmbedder` (implements `IEmbedderBatch`, which extends `IEmbedder`).
- Move the two source files into the new packages via `git mv`.
- Add the new packages as dependencies to `@mcp-abap-adt/llm-agent-server` (openai-llm) and `@mcp-abap-adt/llm-agent` (openai-embedder).
- Re-export `OpenAIProvider` from `@mcp-abap-adt/llm-agent-server`'s public surface for back-compat.
- Re-export `OpenAiEmbedder` from `@mcp-abap-adt/llm-agent`'s public surface for back-compat.
- Rewire `DeepSeekProvider` (stays in server) to import `OpenAIProvider` from `@mcp-abap-adt/openai-llm`.
- Update `embedder-factories.ts` (in core) to import `OpenAiEmbedder` from `@mcp-abap-adt/openai-embedder`.
- Update the `fixed` changesets group to include the four packages so all four stay in lock-step for the 10.x lifecycle.
- Bump `llm-agent`, `llm-agent-server` to 10.1.0. Initial `openai-llm`, `openai-embedder` release at 10.1.0.
- Docs: add `MIGRATION-v10.1.md` noting the new canonical import paths and explaining that old paths still work.

### Out of scope

- Other LLM providers (Anthropic, DeepSeek, SAP AI Core) — planned for 10.2.0+.
- Other embedders (Ollama, SAP AI Core) — planned for 10.3.0+.
- QdrantRag extraction — planned for 10.6.0 (closes the last heavy runtime dep in core).
- Any behavioral change to `OpenAIProvider` or `OpenAiEmbedder` — no logic edits.
- Removing deprecated re-exports — future major bump (v11).

## Resolved questions

| # | Question | Decision |
|---|---|---|
| 1 | Split OpenAI LLM from Embedder? | Yes. Two packages. |
| 2 | Include DeepSeek in this release? | No. `DeepSeekProvider` stays in server, importing `OpenAIProvider` from `@mcp-abap-adt/openai-llm`. DeepSeek extraction is 10.3.0. |
| 3 | Back-compat for v10.0 import paths? | Full re-exports from server and core. Old code keeps compiling. No deprecation warnings in 10.1.0. |
| 4 | Changesets fixed group | Extend to `[[llm-agent, llm-agent-server, openai-llm, openai-embedder]]`. All four bump together for the 10.x lifecycle. |
| 5 | Publish mechanics | `npx changeset publish` publishes all four packages in dependency order. Manual per-package `npm publish` is not needed. |

## Package layouts

### `@mcp-abap-adt/openai-llm`

```
packages/openai-llm/
├── package.json
├── tsconfig.json
├── README.md
├── CHANGELOG.md (generated by changesets)
└── src/
    ├── index.ts                    export { OpenAIProvider }
    └── openai-provider.ts           moved from packages/llm-agent-server/src/llm-providers/openai.ts
```

`package.json`:

```json
{
  "name": "@mcp-abap-adt/openai-llm",
  "version": "10.1.0",
  "description": "OpenAI LLM provider implementing ILlm. Calls /v1/chat/completions and /v1/completions.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "tsc -p tsconfig.json --clean",
    "test": "node --import tsx/esm --test --test-reporter=spec 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@mcp-abap-adt/llm-agent": "*",
    "axios": "^1.14.0"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fr0ster/llm-agent.git"
  },
  "publishConfig": { "access": "public" }
}
```

`tsconfig.json`:

```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    "outDir": "dist",
    "lib": ["ES2022", "DOM"],
    "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["**/__tests__/**", "**/*.test.ts", "dist"],
  "references": [{ "path": "../llm-agent" }]
}
```

### `@mcp-abap-adt/openai-embedder`

```
packages/openai-embedder/
├── package.json
├── tsconfig.json
├── README.md
└── src/
    ├── index.ts                    export { OpenAiEmbedder, type OpenAiEmbedderConfig }
    └── openai-embedder.ts          moved from packages/llm-agent/src/rag/openai-embedder.ts
```

`package.json`:

```json
{
  "name": "@mcp-abap-adt/openai-embedder",
  "version": "10.1.0",
  "description": "OpenAI embedding provider implementing IEmbedder. Calls /v1/embeddings.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md", "LICENSE"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "tsc -p tsconfig.json --clean",
    "test": "node --import tsx/esm --test --test-reporter=spec 'src/**/*.test.ts'"
  },
  "dependencies": {
    "@mcp-abap-adt/llm-agent": "*"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/fr0ster/llm-agent.git"
  },
  "publishConfig": { "access": "public" }
}
```

`OpenAiEmbedder` uses native `fetch` (not `axios`), so no HTTP client runtime dependency is needed. The class implements `IEmbedderBatch` (the batch-capable extension of `IEmbedder`) — the package must export both the class and its `OpenAiEmbedderConfig` type so consumers get the batch API surface.

`tsconfig.json`: same shape as `openai-llm`.

## Changes to existing packages

### `@mcp-abap-adt/llm-agent` (core)

- Add `@mcp-abap-adt/openai-embedder: "*"` to `dependencies` (so the transitive `OpenAiEmbedder` re-export resolves).
- Update `src/rag/embedder-factories.ts` — replace internal `import { OpenAiEmbedder } from './openai-embedder.js'` with `import { OpenAiEmbedder } from '@mcp-abap-adt/openai-embedder'`.
- Update `src/rag/index.ts` — replace `export * from './openai-embedder.js'` (if present) with `export { OpenAiEmbedder, type OpenAiEmbedderConfig } from '@mcp-abap-adt/openai-embedder'`.
- Remove `src/rag/openai-embedder.ts` (it's `git mv`-ed out).
- Remove the re-export from the `rag` barrel that pointed at the old file path.

### `@mcp-abap-adt/llm-agent-server` (server)

- Add `@mcp-abap-adt/openai-llm: "*"` to `dependencies`.
- Every import of the local `./llm-providers/openai.js` path must change to `'@mcp-abap-adt/openai-llm'`. Confirmed import sites (grep of the current tree):
  - `packages/llm-agent-server/src/llm-providers/deepseek.ts` — `DeepSeekProvider extends OpenAIProvider`.
  - `packages/llm-agent-server/src/llm-providers/index.ts` — barrel.
  - `packages/llm-agent-server/src/index.ts` — public re-export.
  - `packages/llm-agent-server/src/smart-agent/providers.ts` — composition root that wires LLM providers by name.
  - `packages/llm-agent-server/src/agents/openai-agent.ts` — agent-hierarchy class that depends on the provider.
  - `packages/llm-agent-server/src/llm-providers/__tests__/**` — any OpenAI provider tests move with the file.
- After updates, `src/index.ts` continues to export `OpenAIProvider` and `OpenAIConfig` via the llm-providers barrel; the public symbol list is unchanged. Verify by `grep OpenAIProvider` on the built `dist/index.d.ts` before/after.
- Remove `src/llm-providers/openai.ts` via `git mv` into the new package (preserves blame). Do NOT leave a re-export stub at the old path — the llm-providers barrel handles back-compat.

## Changesets configuration

### Update `.changeset/config.json`

```json
{
  "fixed": [
    [
      "@mcp-abap-adt/llm-agent",
      "@mcp-abap-adt/llm-agent-server",
      "@mcp-abap-adt/openai-llm",
      "@mcp-abap-adt/openai-embedder"
    ]
  ]
}
```

All four packages stay in lock-step through 10.x. Any non-trivial release bumps all four, even if the change touches only one. Rationale: the four packages form a tight cluster where internal deps are pinned to `*`. Version drift risks silent breakage; lock-step is safe.

When future providers are extracted (10.2.0+), they join the fixed group as well.

### Changeset file for this release

`.changeset/10.1-extract-openai.md`:

```markdown
---
"@mcp-abap-adt/llm-agent": minor
"@mcp-abap-adt/llm-agent-server": minor
---

Extract OpenAI LLM and embedder into dedicated packages.

New packages (shipped at 10.1.0):
- `@mcp-abap-adt/openai-llm` — OpenAIProvider (LLM).
- `@mcp-abap-adt/openai-embedder` — OpenAiEmbedder.

Existing import paths keep working via re-exports. Consumers who want a lighter dependency footprint can import directly from the new packages.

DeepSeekProvider stays in `@mcp-abap-adt/llm-agent-server` and now imports OpenAIProvider from the new package transparently.
```

Both new packages don't need their own entries because changesets auto-adds them to the bump list via the `fixed` group once they appear in workspaces with a populated `package.json`. First publish at 10.1.0.

## Release flow

1. Implement changes (Tasks 1-6 in the plan).
2. `npx changeset` — add the changeset file documented above.
3. `npx changeset version` — bumps all four packages to 10.1.0, writes per-package CHANGELOG.md, removes the changeset file.
4. `npm run build && npm test` — verify.
5. Commit bump, merge PR.
6. Post-merge: `npx changeset publish` publishes all four packages in dependency order in one command.
7. Tag `v10.1.0`, push — GitHub release workflow creates the release page.

## Migration for consumers — `docs/MIGRATION-v10.1.md`

Short doc (≤ 40 lines). Contents:

- **What's new:** two dedicated packages for OpenAI LLM and embedder.
- **What stays the same:** all existing import paths still work — re-exports preserve v10.0 surface.
- **Lighter deps example:** "If you use DeepSeek LLM and Ollama embeddings, you can now write `import { OllamaEmbedder } from '@mcp-abap-adt/llm-agent'` without pulling OpenAI code — install only `@mcp-abap-adt/llm-agent-server @mcp-abap-adt/ollama-embedder` (when available)."
- **New canonical paths:**
  - `import { OpenAIProvider } from '@mcp-abap-adt/openai-llm'`
  - `import { OpenAiEmbedder } from '@mcp-abap-adt/openai-embedder'`
- **Deprecation note:** old paths not deprecated in 10.1. Removed in a future major.
- **Roadmap reminder:** other providers follow in 10.2, 10.3, 10.4.

## Testing

- Core tests continue to pass; `openai-embedder` tests move with the file into `packages/openai-embedder/src/__tests__/`.
- Server tests continue to pass; `openai-provider` tests move into `packages/openai-llm/src/__tests__/`.
- New package tests run via each package's `npm test`.
- Full workspace `npm test` runs all four packages.

## Known debt

- `@mcp-abap-adt/llm-agent` still transitively lists `@mcp-abap-adt/openai-embedder` as a direct dependency (so the back-compat re-export resolves). That's not meaningful progress on "lightweight core" for consumers who only install core. Real dep shrinkage happens when the re-exports are removed so consumers opt into the providers they actually need — a v11 breaking change.
- `axios` remains in core because `QdrantRag` depends on it. Resolved by the planned 10.6.0 release that extracts `QdrantRag` into its own package.
- Workspace publish order depends on `changeset publish` reading the `dependencies` graph correctly. If it doesn't, manual ordering: `openai-llm` and `openai-embedder` first, then core, then server.

## Root build / project-reference updates

`tsc -b packages/llm-agent packages/llm-agent-server` (current root `build` script) does not know about the new packages. Update:

1. Root `package.json` `build`/`clean` scripts — extend the list, with explicit order:
   ```
   "build": "tsc -b packages/openai-embedder packages/llm-agent packages/openai-llm packages/llm-agent-server"
   ```
   `openai-embedder` builds **before** `llm-agent` because core's back-compat re-export needs openai-embedder's `.d.ts` available via node_modules. Then `llm-agent`, then `openai-llm` (depends on core), then `llm-agent-server` (depends on openai-llm and core).
2. `packages/llm-agent-server/tsconfig.json` — add `{ "path": "../openai-llm" }` to `references`. Server depends on openai-llm (one-directional).
3. **Do NOT add `{ "path": "../openai-embedder" }` to `packages/llm-agent/tsconfig.json` `references`.** `openai-embedder` already references `llm-agent` (it implements `IEmbedderBatch` from core), so adding the reverse edge creates a cycle in the TypeScript project graph. The back-compat re-export in core resolves openai-embedder's `.d.ts` via npm workspace symlinks (`node_modules/@mcp-abap-adt/openai-embedder/dist/index.d.ts`) at type-check time; no project reference is needed for correctness. The build-order constraint is enforced by the root script (step 1), not by `tsc -b`'s dependency graph.
4. **openai-llm has the same one-directional relationship** with core (it imports `ILlm`, `Message`, etc.), but no re-export from core back to openai-llm — so no cycle risk on that side. Reference is clean.

Trade-off of skipping the project reference between core and openai-embedder: `tsc -b` incremental rebuilds don't cascade from openai-embedder changes into core automatically. CI always runs clean builds; local dev can `npm run clean && npm run build` after cross-package edits. Acceptable cost to avoid the cycle.

## Open items for implementation plan

- `exports` field of `@mcp-abap-adt/llm-agent` — verify no deep-path exports reference `openai-embedder` internals. If any exist, they either update to re-export from the new package or are removed.

## Review notes and proposals

- **Observation:** the release flow assumes root `npm run build` validates all four packages. **Proposal:** extend the root TypeScript build/clean orchestration and project references to include `packages/openai-llm` and `packages/openai-embedder`, otherwise root verification may stay green while the new packages are not built.
- **Observation:** the server impact list is incomplete. Beyond `deepseek.ts` and `llm-providers/index.ts`, current imports also exist in `src/index.ts`, `src/smart-agent/providers.ts`, `src/agents/openai-agent.ts`, CLI wiring, and OpenAI provider tests. **Proposal:** expand the implementation scope to enumerate every server import site that currently points to `./llm-providers/openai.js`.
- **Observation:** the `openai-embedder` package draft lists `axios` as a dependency, and the known-debt section says core still pulls `axios` transitively through that package. **Proposal:** remove that claim unless the implementation is intentionally rewritten; the current `OpenAiEmbedder` uses native `fetch`, so the package should not add `axios` without a deliberate design change.
- **Observation:** the spec says `OpenAiEmbedder` implements `IEmbedder`, while the current class implements `IEmbedderBatch`. **Proposal:** use the more precise contract name in the spec so the extraction preserves the existing batch-capable API surface explicitly.
- **Observation:** one "open item" is already resolved in the current codebase: `embedder-factories.ts` already registers the `openai` embedder factory. **Proposal:** replace that open question with a concrete task to update the import path only, and keep the remaining open item focused on package exports validation.
- **Observation:** adding `packages/openai-embedder` to `packages/llm-agent/tsconfig.json` `references` would create a circular TypeScript project graph, because the spec already makes `openai-embedder` reference `llm-agent`. **Proposal:** do not model the back-compat re-export via a new reverse project reference. Either keep the packages acyclic by moving shared RAG interfaces lower in the graph, or choose a different compatibility mechanism that does not require `llm-agent -> openai-embedder -> llm-agent`.
